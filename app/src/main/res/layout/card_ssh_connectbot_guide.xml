<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="16dp"
    android:backgroundTint="@color/white"
    app:cardCornerRadius="24dp"
    app:cardElevation="8dp"
    android:stateListAnimator="@animator/button_elevation">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="24dp">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_terminal_advanced"
            app:tint="#2D3436"
            android:layout_gravity="center_vertical"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginStart="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="10. 進階操作：使用專業 SSH App"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="#2D3436"
                android:layout_marginBottom="8dp"/>

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="若您需要複製貼上文字、管理多個視窗或執行更複雜的指令，推薦您使用功能更完整的 SSH App。以下將以開源且強大的 ConnectBot (由 Kenny Root 開發) 為例進行教學。\n\n操作步驟：\n1. 獲取連線資訊\n首先，請在本 App 的 USB 終端機中，獲取連線所需的資訊：\n  • 使用者名稱：在命令列輸入 logname 並發送，您將會看到使用者名稱 (例如 pi、sivs或其他使用者名稱)。\n  • IP 位址：接著輸入 ip addr 並發送。在 usb0 或類似介面的 inet 欄位找到一組 192.168.x.x 格式的 IP 位址。\n\n2. 使用 ConnectBot 連線\n在保持 USB 網路共享開啟的狀態下，打開 ConnectBot App：\n  • 在主畫面的連線欄位中，輸入 使用者名稱@IP位址 的格式，例如：pi@192.168.42.50\n  • ConnectBot 會自動使用標準的 SSH 埠號 (Port 22)，您無需手動設定。\n\n3. 輸入密碼\n當提示輸入密碼時，請輸入解魔方機的樹莓派預設密碼：\n151932\n\n完成後，您就可以透過 ConnectBot 享受完整的 SSH 終端機體驗了。"
                android:textSize="16sp"
                android:textColor="#636e72"
                android:lineSpacingExtra="4dp"/>
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>